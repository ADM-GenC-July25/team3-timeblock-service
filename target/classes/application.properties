# Application Configuration
spring.application.name=timeblock-service
server.port=8084

# Database Configuration - AWS RDS MySQL
# Environment variables for Elastic Beanstalk deployment
spring.datasource.url=jdbc:mysql://${DB_HOST:mysql-training.cd4qsem4uf1x.us-west-2.rds.amazonaws.com}:${DB_PORT:3306}/${DB_NAME:team3capstonedb}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=${DB_USERNAME:admin}
spring.datasource.password=${DB_PASSWORD:Be.Cognizant2025!}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Eureka Client Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.hostname=localhost
eureka.instance.prefer-ip-address=true
eureka.instance.instance-id=${spring.application.name}:${server.port}

# Actuator Configuration for Health Checks
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# CORS Configuration
cors.allowed-origins=http://localhost:3000,http://localhost:5173,https://2362857-team3-bucket.s3-website-us-west-2.amazonaws.com,https://2362857-team3-bucket-main.s3-website-us-west-2.amazonaws.com

# Logging Configuration
logging.level.com.scheduleplanner.timeblock=DEBUG
logging.level.org.springframework.cloud.netflix.eureka=INFO 